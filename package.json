{
  "name": "release-please-poc",
  "version": "1.0.0",
  "description": "A Proof of Concept for release-please",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.0",
    "@commitlint/config-conventional": "^17.0.0",
    "husky": "^9.1.7"
  }
}
